import React from "react";
import classes from "./SearchPage.module.scss";

const SearchPage = () => {
  return (
    <div className={classes.main}>
      <h2 className={classes.title}>Search "boston"</h2>
    </div>
  );
};

export default SearchPage;
